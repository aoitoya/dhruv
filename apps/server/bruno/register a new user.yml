info:
  name: register a new user
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-up/email"
  body:
    type: json
    data: |-
      {
        "name": "Toya",
        "email": "toya@dhruv.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
