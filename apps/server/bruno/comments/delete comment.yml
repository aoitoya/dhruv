info:
  name: delete comment
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/comments/{{commentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5