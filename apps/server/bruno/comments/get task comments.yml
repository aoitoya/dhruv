info:
  name: get task comments
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/tasks/{{taskId}}/comments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5