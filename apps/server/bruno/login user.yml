info:
  name: login user
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-in/email"
  body:
    type: json
    data: |-
      {
        "email": "toya@dhruv.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
