info:
  name: get workspace projects
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/workspaces/{{workspaceId}}/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5