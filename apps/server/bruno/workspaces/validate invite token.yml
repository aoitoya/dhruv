info:
  name: validate invite token
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/invites/validate/{{token}}"
  auth: inherit

runtime:
  variables:
    - name: token
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5