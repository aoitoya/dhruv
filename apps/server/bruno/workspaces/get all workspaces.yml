info:
  name: get all workspaces
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/workspaces"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
