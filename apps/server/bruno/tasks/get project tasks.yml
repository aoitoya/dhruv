info:
  name: get project tasks
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/projects/{{projectId}}/tasks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5