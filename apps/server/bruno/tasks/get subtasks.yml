info:
  name: get subtasks
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/tasks/{{taskId}}/subtasks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5