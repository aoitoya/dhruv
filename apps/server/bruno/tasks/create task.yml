info:
  name: create task
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/projects/{{projectId}}/tasks"
  body:
    type: json
    data: |-
      {
        "title": "Fix bug"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5