info:
  name: get task by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/tasks/{{taskId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5