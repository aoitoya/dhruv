info:
  name: reorder task
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{baseUrl}}/api/tasks/{{taskId}}/reorder"
  body:
    type: json
    data: |-
      {
        "position": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5