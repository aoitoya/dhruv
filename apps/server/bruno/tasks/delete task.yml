info:
  name: delete task
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/tasks/{{taskId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5