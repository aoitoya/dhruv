info:
  name: get project members
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/projects/{{projectId}}/members"
  auth: inherit

runtime:
  variables:
    - name: projectId
      value: abc123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
