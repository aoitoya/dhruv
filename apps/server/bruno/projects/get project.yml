info:
  name: get project by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/projects/{{projectId}}"
  auth: inherit

runtime:
  variables:
    - name: projectId
      value: abc123

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
