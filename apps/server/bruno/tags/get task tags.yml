info:
  name: get task tags
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/tasks/{{taskId}}/tags"
  auth: inherit

runtime:
  variables:
    - name: taskId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5